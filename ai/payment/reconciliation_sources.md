# Reconciliation Sources

## Supported Inputs

### Manual Input (v1)
Admin pastes transaction message into system

### Email Parsing (future)
System reads bank notification emails

### SMS Forwarding (future)
System receives forwarded SMS messages

## Rule
All inputs must be parsed into structured data before matching